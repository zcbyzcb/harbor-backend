<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.harbor.hotel.infrastructure.persistence.mapper.RoomMapper">
  <select id="findActiveByRoomTypeId" resultType="com.harbor.hotel.infrastructure.persistence.po.RoomPO">
    SELECT id, room_type_id AS roomTypeId, room_no AS roomNo, physical_status AS physicalStatus FROM room WHERE room_type_id = #{roomTypeId} AND is_deleted = 0 ORDER BY id
  </select>
  <select id="findByIds" resultType="com.harbor.hotel.infrastructure.persistence.po.RoomPO">
    SELECT id, room_type_id AS roomTypeId, room_no AS roomNo, physical_status AS physicalStatus FROM room WHERE is_deleted = 0 AND id IN <foreach collection="ids" item="id" open="(" separator="," close=")">#{id}</foreach> ORDER BY id
  </select>
  <update id="markOccupied">
    UPDATE room SET physical_status = 'OCCUPIED' WHERE id = #{id} AND physical_status = 'READY' AND is_deleted = 0
  </update>
</mapper>
