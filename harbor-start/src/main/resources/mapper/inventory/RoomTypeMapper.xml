<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.harbor.hotel.infrastructure.persistence.mapper.RoomTypeMapper">
  <select id="findActive" resultType="com.harbor.hotel.infrastructure.persistence.po.RoomTypePO">
    SELECT id, type_name AS name, base_price AS price, max_guests AS maxGuests FROM room_type WHERE is_deleted = 0 ORDER BY id
  </select>
  <select id="findById" resultType="com.harbor.hotel.infrastructure.persistence.po.RoomTypePO">
    SELECT id, type_name AS name, base_price AS price, max_guests AS maxGuests FROM room_type WHERE id = #{id} AND is_deleted = 0
  </select>
</mapper>
